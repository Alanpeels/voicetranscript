# Try this if the full version still fails
flask==3.1.2
flask-cors==4.0.0
gunicorn==23.0.0
torch==2.0.1+cpu  # CPU-only version
transformers==4.30.0  # Older, more stable version
librosa==0.9.2  # Lighter version
numpy==1.24.4
soundfile==0.12.1